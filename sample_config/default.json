{
    "config": {
        "server": {
            "port": 80,
            "host": "0.0.0.0"
        },
        "docker-mailserver": "mailserver",
        "paths": {
            "dsm-config": "/tmp/docker-mailserver",
            "dsm-mail-data": "/var/mail",
            "dsm-mail-state": "/var/mail-state/",
            "dsm-mail-logs": "/var/log/mail"
        },
        "register" : true,
        "change_pass" : true
    }
}