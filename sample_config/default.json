{
    "config": {
        "server": {
            "port": 8080,
            "host": "0.0.0.0"
        },
        "docker-mailserver": "mailserver",
        "paths": {
            "dsm-config": "./dms_data/config",
            "dsm-mail-data": "/var/mail/",
            "dsm-mail-state": "/var/mail-state/",
            "dsm-mail-logs": "/tmp/docker-mailserver/"
        }
    }
}